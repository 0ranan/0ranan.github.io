---
title: qemu 训练营:rust 函数与代码块
date: 2026/08/25 08:17:41
updated: 2026/09/03 15:41:30
comments: true
tags:
  - rust
  - qemu训练营
categories:
  - 编程
typora-root-url: ..
---

# qemu 训练营:rust 函数与代码块

## 函数的基本语法和注意点

- Rust 代码中的函数名和变量名通常使用 snake case 风格。在 snake case 中，所有字母都使用小写，并用下划线分隔单词。

```rust
fn main() {
    println!("Hello, world!");
    another_function();
}

fn another_function() {
    println!("Another function.");
}
```



## 函数的参数

Rust 中定义函数如果需要具备参数必须声明参数名称和类型。

```rust
fn main() {
    another_function(5);
}

fn another_function(x: i32) {
    println!("The value of x is: {x}");
}
```

## 函数的返回值

```rust
fn main() {
    fn hello8() -> i32 {
        8
    }
    println!("hello8 {}",hello8());
    println!("hello9 {}",hello9());
}

fn hello9() -> i32 {
    9
}
```



## 表达式，语句，块

rust 中 当程序块的末尾如果没有分号，那么程序块就是一段语句。语句的返回至就是最后一行的值。

```rust 
fn main() {
    let x = 5;

    let y = {
        let x = 3;
        x + 1
    };

    println!("x 的值为 : {}", x);
    println!("y 的值为 : {}", y);
}
```

## 参考链接

- [Rust 基本语法 - 菜鸟教程](https://www.runoob.com/rust/rust-basic-syntax.html)
- [qemu 训练营 - Rust 变量与可变性](https://opencamp.cn/gevico/camp/2026/stage/5)
- [Variables and Mutability - The Rust Programming Language](https://doc.rust-lang.org/book/ch03-01-variables-and-mutability.html)
